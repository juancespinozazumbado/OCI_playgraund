# # This workflow will build a .NET project
# # For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

# name: build

# on:
#   push:
#     branches: 
#     - main
#   pull_request:
#     branches:
#     - main
# env:
#   NODE_ENV: production
#   PORT: 4000


# jobs:
#   build:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4
#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: 20.x
#     - name: Install dependencies
#       run: npm install
#     # - name: Build
#     #   run: npm run build



# name: Build, Push & Deploy to OCI

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Log in to OCIR
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ secrets.OCIR_REGION }}.ocir.io
#           username: ${{ secrets.OCIR_USERNAME }}
#           password: ${{ secrets.OCIR_PASSWORD }}

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build & push Docker image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: ./Dockerfile
#           push: true
#           platforms: linux/arm64          
#           tags: |
#             ${{ secrets.OCIR_REGION }}.ocir.io/${{ secrets.OCIR_NAMESPACE }}/my-rest-api:latest
#             ${{ secrets.OCIR_REGION }}.ocir.io/${{ secrets.OCIR_NAMESPACE }}/my-rest-api:${{ github.sha }}

name: Deploy to Azure App Service

on:
  push: 
    branches: 
      - main

jobs: 
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3
      - name: Setup Node.js version
        uses: actions/setup-node@v4
        with: 
          node-version: '18.x'
      - name: Install Dependencies
        run: npm install
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: bots-api
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .  